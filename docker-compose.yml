version: "3.9"
services:
  praktomat:
    build: praktomat
    ports:
      - 443:443
    volumes:
      - workdata:/home/praktomat/work-data
    depends_on:
      - postgresql
  postgresql:
    build: postgresql
    environment:
      POSTGRES_PASSWORD: superuser_password
volumes:
  workdata:
